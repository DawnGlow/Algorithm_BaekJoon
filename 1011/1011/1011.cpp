//incompletion code
#include <iostream>
using namespace std;
main () {
    int T;
    cin >> T;
    for (int i = 0; i++ < T;) {
        int x, y, k, m(1);
        cin >> x >> y;
        while (x + k != y) {
            
        }
    }
}
